import React, { Component } from 'react';
import { View, Text } from 'react-native';

class LeroLeroScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <View>
        <Text> textInComponent </Text>
        {/* // Titulo
        // Numero de frases
        // Tema
        // Gerador
        // Botoes de pdf
        // Botao de compartilhar
        // Texto gerado */}
      </View>
    );
  }
}

export default LeroLeroScreen;
